<h1>index</h1>

<% @tasks.each do |task| %>
<div class="test">
<p><%= link_to "#{task.title}", task_path(task) %>
<%= link_to "#{task.details}", task_path(task)%>
<%= link_to "#{task.completed}", task_path(task)%>
</div>
<% end %>

<%= link_to'nouveaux', tasks_new_path %>
